pkgname=codex-army
pkgver=0.151.0
pkgrel=1
pkgdesc='Codex CLI fork with account-switch failover'
arch=('x86_64')
url='https://github.com/sieciowiecxyz/codex-army'
license=('Apache-2.0')
depends=('glibc')
conflicts=('codex')
options=(!debug)
source=('codex' 'codex-code-mode-host' 'LICENSE')
# The release workflow replaces these values with the checksums of the staged
# binaries and license before running makepkg.
sha256sums=('SKIP' 'SKIP' 'SKIP')

package() {
  install -Dm755 "$srcdir/codex" "$pkgdir/usr/bin/codex"
  install -Dm755 "$srcdir/codex-code-mode-host" "$pkgdir/usr/bin/codex-code-mode-host"
  install -Dm644 "$srcdir/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
